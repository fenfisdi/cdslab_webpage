import { makeStyles } from '@material-ui/core/styles'

export const useFixedParametersStyles = makeStyles(() => ({
  root: {
    height: '100vh',
  },
  body: {
    marginTop: '20px',
  },
  title:{
    marginBottom:'20px',
    marginTop:'20px'
  }
}))