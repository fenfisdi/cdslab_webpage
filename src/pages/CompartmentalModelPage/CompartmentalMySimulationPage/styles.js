import { makeStyles } from '@material-ui/core/styles'

export const useCompartamentalMySimulationForm = makeStyles(() => ({ 
  columns:{
    border: '1px solid #eee',
  },
  iconPreview:{
    cursor: 'pointer'
  }
}))
