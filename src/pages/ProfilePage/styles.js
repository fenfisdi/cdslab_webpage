import { makeStyles } from '@material-ui/core/styles'

export const useProfilePageStyles = makeStyles(() => ({
  root: {
    flexGrow: 1,
    marginTop: '5%',
    marginRight: '10%'
  },
  item: {
    marginRight: '10%'
  },
}))